package com.example.chat_app.enums;

/**
 * Type of chat conversation
 */
public enum ChatType {
    P2P, // Peer-to-peer (direct message between 2 users)
    GROUP // Group chat with multiple users
}
